import 'package:flutter/material.dart';

class SuppliesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Supplies')),
      body: const Center(child: Text('Supplies Screen')),
    );
  }
}